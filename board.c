#include <stdint.h>
#include "board88.h"
#include "bitboard.h"
#include <stdio.h>
// macros for board88

unsigned char board_88[128];
//const unsigned char dist_man[240];

int main(){
    for(int i = 0; i < 240; i++)
        printf("%d", dist[i]);
}

